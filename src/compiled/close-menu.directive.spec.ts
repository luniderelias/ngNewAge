import { CloseMenuDirective } from './close-menu.directive';

describe('CloseMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseMenuDirective(null);
    expect(directive).toBeTruthy();
  });
});
